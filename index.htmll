<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Sahil Rajheti — Portfolio</title>
  <meta name="description" content="Sahil Rajheti — personal portfolio">
  <style>
    /* Basic reset & layout */
    :root{--accent:#0ea5a4;--bg:#0f172a;--card:#0b1220;--glass:rgba(255,255,255,0.03)}
    *{box-sizing:border-box}html,body{height:100%;}
    body{font-family:Inter,system-ui,Segoe UI,Roboto,Arial;line-height:1.5;margin:0;background:linear-gradient(180deg,#021024 0%, #071029 100%);color:#e6eef6;-webkit-font-smoothing:antialiased;scroll-behavior:smooth}
    .container{max-width:980px;margin:32px auto;padding:20px}
    header{display:flex;align-items:center;gap:16px}
    .avatar{width:88px;height:88px;border-radius:14px;overflow:hidden;flex:0 0 88px;background:var(--glass);display:flex;align-items:center;justify-content:center}
    .avatar img{width:100%;height:100%;object-fit:cover}
    h1{font-size:clamp(20px,3.4vw,30px);margin:0}
    p.lead{margin:6px 0 0;color:#bcd3e6}

    nav{margin-top:18px;display:flex;gap:12px;flex-wrap:wrap}
    .btn{background:transparent;border:1px solid rgba(255,255,255,0.06);padding:8px 12px;border-radius:10px;color:inherit;text-decoration:none;font-weight:600}

    section{margin-top:28px;background:var(--card);padding:18px;border-radius:14px;box-shadow:0 6px 18px rgba(2,6,23,0.6)}
    .grid{display:grid;grid-template-columns:1fr 320px;gap:18px}
    @media (max-width:880px){.grid{grid-template-columns:1fr}}

    .skills{display:flex;flex-wrap:wrap;gap:8px}
    .chip{padding:6px 10px;border-radius:999px;background:rgba(255,255,255,0.03);border:1px solid rgba(255,255,255,0.03)}

    .projects-list{display:grid;gap:10px}
    .project{padding:12px;border-radius:10px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));border:1px solid rgba(255,255,255,0.03)}

    footer{margin-top:22px;text-align:center;color:#9fb1c8}

    /* reveal animation */
    .reveal{opacity:0;transform:translateY(18px) scale(0.995);transition:all 520ms cubic-bezier(.2,.9,.25,1)}
    .reveal.visible{opacity:1;transform:none}

    /* simple top nav */
    .topbar{position:sticky;top:12px;margin-bottom:12px;padding:10px;border-radius:12px;background:linear-gradient(180deg,rgba(255,255,255,0.02),transparent);backdrop-filter:blur(6px)}

    /* small utilities */
    .muted{color:#9fb1c8}
    .socials a{margin-right:8px;text-decoration:none}
    .cta{display:inline-block;padding:10px 14px;border-radius:10px;background:var(--accent);color:white;text-decoration:none;font-weight:700}

    /* blog list */
    .blog-list{display:grid;gap:10px}
    .blog-card{padding:12px;border-radius:10px;background:linear-gradient(180deg,rgba(255,255,255,0.01),transparent)}
  </style>
</head>
<body>
  <div class="container">
    <div class="topbar reveal">
      <header>
        <div class="avatar">
          <!-- replace the src with your image file (e.g. static/robot.png or assets/photo.jpg) -->
          <img src="static/robot.png" alt="Sahil Rajheti">
        </div>
        <div>
          <h1>Sahil Rajheti</h1>
          <p class="lead">Web developer • Course creator • Portfolio on GitHub Pages</p>
          <div class="muted">Repo: <a href="https://sahilrajheti.github.io/SahilRajheti/" target="_blank" rel="noopener">sahilrajheti.github.io/SahilRajheti</a></div>
        </div>
      </header>
      <nav>
        <a class="btn" href="#about">About</a>
        <a class="btn" href="#projects">Projects</a>
        <a class="btn" href="#blog">Blog</a>
        <a class="btn" href="#contact">Contact</a>
        <a class="cta" href="#contact">Hire / Admin</a>
      </nav>
    </div>

    <main>
      <section id="about" class="reveal">
        <h2>About Me</h2>
        <p>Hello — I'm <strong>Sahil Rajheti</strong>. I build responsive portfolios, online courses (Class 10 Biology, How to Earn Money Online), and small web apps. This page is ready to upload to your GitHub Pages repo.</p>
        <p class="muted">Quick facts: iPhone 14 Pro user, GitHub Pages site, prefers clear step-by-step instructions.</p>
        <h3>Skills</h3>
        <div class="skills">
          <span class="chip">HTML</span>
          <span class="chip">CSS</span>
          <span class="chip">JavaScript</span>
          <span class="chip">Git & GitHub</span>
          <span class="chip">Responsive Design</span>
          <span class="chip">Course Creation</span>
        </div>
      </section>

      <div class="grid">
        <div>
          <section id="projects" class="reveal">
            <h2>Projects</h2>
            <div class="projects-list">
              <article class="project">
                <h4>Sa eedi-course (example)</h4>
                <p class="muted">A course site you can upload into <code>/saeedi-course/</code> on your repo.</p>
                <a href="#" class="btn">View repo</a>
              </article>
              <article class="project">
                <h4>Portfolio Website</h4>
                <p class="muted">This exact file (index.html) — copy to repo root or a folder.</p>
                <a href="#" class="btn">Customize</a>
              </article>
            </div>
          </section>

          <section id="blog" class="reveal" style="margin-top:18px">
            <h2>Blog (placeholder)</h2>
            <div class="blog-list">
              <article class="blog-card">
                <strong>How to upload course to GitHub Pages</strong>
                <div class="muted">Short step-by-step in the contact section below.</div>
              </article>
            </div>
          </section>
        </div>

        <aside>
          <section class="reveal">
            <h3>Contact & Socials</h3>
            <p class="muted">Replace placeholders with your actual handles / email.</p>
            <div class="socials">
              <div><a href="#">Email: you@example.com</a></div>
              <div><a href="#" target="_blank" rel="noopener">LinkedIn</a> • <a href="#">Twitter</a></div>
              <div><a href="#">Telegram</a> • <a href="#">Instagram</a> • <a href="#">Facebook</a></div>
            </div>
            <h4 style="margin-top:10px">Admin</h4>
            <p class="muted">If you want an admin panel link, add it here.</p>
          </section>
        </aside>
      </div>

      <section id="contact" class="reveal">
        <h2>How to upload this file to your GitHub Pages repo</h2>
        <ol>
          <li>Open your repository on GitHub: <code>https://github.com/sahilrajheti/SahilRajheti</code> (or navigate to your repo).</li>
          <li>Option A — GitHub web: Click <strong>Add file &gt; Create new file</strong>, name it <code>index.html</code> (or place inside a folder), paste the contents of this file and commit to <code>main</code> branch.</li>
          <li>Option B — Local git (recommended):
            <pre>
git clone https://github.com/sahilrajheti/SahilRajheti.git
cd SahilRajheti
# copy this index.html into the folder
git add index.html
git commit -m "Add portfolio index.html"
git push origin main
            </pre>
          </li>
          <li>Enable GitHub Pages (if not already): Repository &gt; Settings &gt; Pages &gt; Source: <strong>main branch / root</strong> &gt; Save. Your site will be available at <code>https://sahilrajheti.github.io/SahilRajheti/</code>.</li>
          <li>Replace placeholders: update <code>static/robot.png</code> with your photo (upload into repo), update email and social links.</li>
          <li>To show this file inside a folder (e.g. <code>/saeedi-course/</code>), create a folder with that name and upload <code>index.html</code> there — the URL becomes <code>https://sahilrajheti.github.io/SahilRajheti/saeedi-course/</code>.</li>
        </ol>

        <p class="muted">Extra: If you use the GitHub Desktop app or VS Code, you can drag & drop files, then commit & push via UI.</p>
      </section>

    </main>

    <footer class="reveal">
      <div class="muted">Made with ❤️ — Sahil Rajheti</div>
    </footer>

  </div>

  <script>
    // Reveal on scroll (small, fast, dependency-free)
    const io = new IntersectionObserver((entries)=>{
      entries.forEach(e=>{
        if(e.isIntersecting) e.target.classList.add('visible');
      });
    },{threshold:0.12});
    document.querySelectorAll('.reveal').forEach(el=>io.observe(el));

    // Smooth mobile nav (optional enhancement)
    document.querySelectorAll('nav a').forEach(a=>{
      a.addEventListener('click', (ev)=>{
        const href = a.getAttribute('href');
        if(href && href.startsWith('#')){
          ev.preventDefault();
          document.querySelector(href)?.scrollIntoView({behavior:'smooth',block:'start'});
        }
      });
    });
  </script>

  <!--
    INSTRUCTIONS / NOTES (visible inside the file):
    1) Replace static/robot.png with your actual photo file. You can upload into repo a folder named 'static' and add robot.png.
    2) Replace social links and email placeholders with your actual handles.
    3) If you want this page at the repo root, name it index.html and push to main. If you want it inside a folder (e.g. /saeedi-course/), create that folder and place the file there.
    4) If you want help customizing content (Hindi/English versions, adding exam questions, or converting into a course layout), tell me what to add and I'll update this file.
  -->
</body>
</html>
